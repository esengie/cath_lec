-----------------------------------------------------------------
1.
1) макс элемент
2) min(A, B)
-----------------------------------------------------------------
2. Возьмем парой объект A со стрелками !_A и id_A
-----------------------------------------------------------------
3. Из уникальности стрелки в 1
-----------------------------------------------------------------
4.
1) Есть стрелка из А в В. Значит есть уникальная стрелка h : A -> AxB
и pi_1 * h = id_a
2) s * pi_1 = t * pi_1 => s * pi_1 * h = t * pi_1 * h => s = t
-----------------------------------------------------------------
5. Покомпонентное умножение элементов. Проекции очевидные. Терминальный объект единичка.
-----------------------------------------------------------------
6.
а) Ax1 ~ A. Возьмем ev : Ax1 -> A, а именно ev := pi_1
  g : C x 1 -> A, f = C -> A (есть изом между Сх1 и С). Ну и работает.
b) -
-----------------------------------------------------------------
7.
1) K = \x\y -> x = phi(phi(pi_1))
2) S = \x\y\z -> xz(yz) == phi(phi(phi(ev * <
                                             ev * <pi_1, pi_3>,
                                             ev * <pi_2, pi_3>
                                             >)))
-----------------------------------------------------------------
8.
Нат числа бы сюда не простые еще
-----------------------------------------------------------------
9.
-----------------------------------------------------------------
10.

Что значит по каррированию соответствующий??
-----------------------------------------------------------------
11.
zero = zero arrow
suc = suc * (...)
rec =




-------
